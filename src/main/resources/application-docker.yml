spring:
  datasource:
    url: jdbc:mysql://localhost:13306/fail?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: fail
    password: fail

server:
  port: 18080

jwt:
  secret: docker-only-jwt-secret-key-should-be-long-enough-2026
